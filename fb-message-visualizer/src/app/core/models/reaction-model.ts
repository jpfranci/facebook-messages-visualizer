export interface ReactionModel {
    reaction: string,
    displayName: string,
    // frequencies and dates are json strings
    frequencies: string,
    dates: string,
    startDate: string,
    endDate: string
}